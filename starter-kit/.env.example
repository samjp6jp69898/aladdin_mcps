# agrabah 帳密（企劃自己的帳號，只留在這台電腦；正式使用時複製這份檔案為 .env）
#
# 用法：
#   cp .env.example .env
#   然後打開 .env，把下面兩個值填成你自己登入 agrabah 後台的帳號密碼，存檔。
#
# 這裡填的不是 MCP token（token 在 .mcp.json，工程師已經幫你填好了）——
# 是你原本登入 agrabah 後台用的帳號密碼。登入 skill 會從 .env 讀出來，
# 直接在你自己電腦上打後台的 /login，密碼不會出現在跟 Claude 的對話紀錄裡，
# 也不會被存到任何工程師看得到的地方。
#
# 填完之後：
#   - Mac：在終端機對 .env 執行一次 chmod 600（README 有完整指令）。
#   - Windows：檔案總管右鍵 .env → 內容 → 安全性，確認只有你自己這個使用者
#     帳號能讀取（README 有完整步驟）。
#   - 等號兩邊不要留空白（例如不要寫成「AGRABAH_ADMIN_USER = foo」）——
#     登入 skill 是逐行比對「開頭是不是 AGRABAH_ADMIN_USER=」，前面多了
#     空白會讓這一行完全不被辨識，等同沒填。
#   - 不要加引號：等號後面到這一行結尾的所有字元（包含引號本身）都會被
#     原封不動當成密碼使用，不會有任何「去掉引號」的動作。密碼裡如果本來
#     就有空白、`$`、反引號、雙引號這類特殊字元都沒關係，直接打上去即可
#     （登入 skill 只做純文字讀取，不會把 .env 當指令執行，不會有任何字元
#     被 shell 特殊解讀）。
AGRABAH_ADMIN_USER=
AGRABAH_ADMIN_PASSWORD=

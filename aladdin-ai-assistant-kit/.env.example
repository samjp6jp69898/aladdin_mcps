# agrabah 帳密（企劃自己的帳號，只留在這台電腦；正式使用時複製這份檔案為 .env）
#
# 用法：
#   cp .env.example .env
#   然後打開 .env，把你**實際被授權的那幾個後台**的帳號密碼填進去，存檔。
#
# 這裡填的不是 MCP token（token 在 .mcp.json，工程師已經幫你填好了）——
# 是你原本登入 agrabah 後台用的帳號密碼。登入 skill 會從 .env 讀出來，
# 直接在你自己電腦上打後台的 /login，密碼不會出現在跟 Claude 的對話紀錄裡，
# 也不會被存到任何工程師看得到的地方。
#
# ── 你要填哪幾組？打開 .mcp.json 對照 ────────────────────────
# 每一個後台都是完全獨立的系統：不同網址、不同資料、不同的帳號名冊。
# 你在 dev 有帳號，不代表 pre／evi 也有同一組帳密。
#
# 下面把「所有可能的後台」都列出來了，但你**不需要全部填**。
# 打開 .mcp.json 看 mcpServers 底下有哪些名字，就填同名的那幾組：
#
#     .mcp.json 裡的名字          →   要填的欄位
#     ─────────────────────────────────────────────
#     agrabah-admin-dev           →   AGRABAH_ADMIN_DEV_*
#     agrabah-admin-pre           →   AGRABAH_ADMIN_PRE_*
#     agrabah-admin-evi           →   AGRABAH_ADMIN_EVI_*
#     agrabah-platform            →   AGRABAH_PLATFORM_*
#
# 規則就是：名字轉大寫、減號換底線、後面加 _USER 或 _PASSWORD。
# （哪天 .mcp.json 裡出現上面沒列到的名字，照這個規則推出欄位名就對了。）
#
# 沒有出現在你 .mcp.json 裡的環境，對應欄位**留空就好**，不會造成任何錯誤——
# 登入 skill 只會去登入 .mcp.json 裡確實有設定的環境，留空的欄位它連看都不會看。
#
# 就算好幾個環境的帳號密碼剛好一樣，也建議各自填一次。這樣哪天某一個環境
# 單獨改密碼時，你只要改那一組，不會影響到其他環境。
# （真的嫌麻煩，檔案最後面有一組「共用帳密」可以用，但優先順序較低，
#   請先看那一段的說明。）
# ──────────────────────────────────────────────────
#
# 填寫規則（四個常見地雷）：
#   - 等號兩邊不要留空白（不要寫成「AGRABAH_ADMIN_DEV_USER = foo」）——
#     登入 skill 是逐行比對「這一行開頭是不是 AGRABAH_ADMIN_DEV_USER=」，
#     前面多了空白會讓這一行完全不被辨識，等同沒填。
#   - 不要加引號：等號後面到這一行結尾的所有字元（包含引號本身）都會被
#     原封不動當成密碼使用，不會有任何「去掉引號」的動作。密碼裡如果本來
#     就有空白、`$`、反引號、雙引號這類特殊字元都沒關係，直接打上去即可
#     （登入 skill 只做純文字讀取，不會把 .env 當指令執行，不會有任何字元
#     被 shell 特殊解讀）。
#   - 同一組的 _USER 和 _PASSWORD 要嘛都填、要嘛都留空，不要只填一個——
#     只填一半的話，登入 skill 會直接告訴你少填哪一個，並且**不會**替那個
#     環境送出登入請求（送出去必定失敗，而失敗次數會被後台記進「連續失敗
#     5 次鎖 5 分鐘」的計數，不值得白白消耗）。
#   - 填完之後：
#       Mac：在終端機對 .env 執行一次 chmod 600（README 有完整指令）。
#       Windows：檔案總管右鍵 .env → 內容 → 安全性，確認只有你自己這個
#       使用者帳號能讀取（README 有完整步驟）。
#
# 不確定自己被授權哪些環境、或不確定某個後台該用哪一組帳號 → **問發你這份
# kit 的工程師**，不要拿別的環境的帳密去試。各後台帳號名冊互不相通，試錯只
# 會累積登入失敗次數，累積到上限就會被鎖住一段時間。


# ═══════════════════════════════════════════════════
#  系統管理後台（admin）
# ═══════════════════════════════════════════════════

# dev   https://admin.alddev.com
# 對應 .mcp.json 的 agrabah-admin-dev
AGRABAH_ADMIN_DEV_USER=
AGRABAH_ADMIN_DEV_PASSWORD=

# pre / cqa   https://abu-admin.ald777.com
# 對應 .mcp.json 的 agrabah-admin-pre
AGRABAH_ADMIN_PRE_USER=
AGRABAH_ADMIN_PRE_PASSWORD=

# evi   https://admin.godev2.com
# 對應 .mcp.json 的 agrabah-admin-evi
AGRABAH_ADMIN_EVI_USER=
AGRABAH_ADMIN_EVI_PASSWORD=


# ═══════════════════════════════════════════════════
#  平台管理後台（platform）
# ═══════════════════════════════════════════════════

# 單一環境，不分 dev／pre／evi
# 對應 .mcp.json 的 agrabah-platform
AGRABAH_PLATFORM_USER=
AGRABAH_PLATFORM_PASSWORD=


# ═══════════════════════════════════════════════════
#  共用帳密（選填，優先順序最低）
# ═══════════════════════════════════════════════════
# 這一組不是給某個特定後台用的，而是「上面那幾組沒填的環境」的預設值。
#
# 優先順序：登入某個環境時，**先找它自己的那組欄位**（例如登入
# agrabah-admin-dev 時先找 AGRABAH_ADMIN_DEV_USER／AGRABAH_ADMIN_DEV_PASSWORD），
# 那組沒填才回頭用下面這一組。所以上面填了的環境，永遠不會被這組蓋掉。
#
# 什麼時候用得上：你所有後台用的都是同一組帳號密碼，又懶得上面貼好幾次，
# 就可以上面全部留空、只填這一組。
#
# 什麼時候別用：只要有任何一個後台的帳密跟其他的不一樣，就請乖乖用上面
# 那幾組分開填——混著用很容易讓人搞不清楚某個環境現在到底是用哪一組登入的。
AGRABAH_ADMIN_USER=
AGRABAH_ADMIN_PASSWORD=
